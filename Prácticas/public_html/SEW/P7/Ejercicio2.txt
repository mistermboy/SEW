EJERCICIO 2:
	-Ejercicio 46 (sacar datos de un JSON)
	-Ejercicio 47(crear métodos a los objetos JSON)
	-Ejercicio 45(leer un archivo de usuarios en formato JSON)
	-Ejercicio 44(Consumir servicios web) (===  comprueba identidad total)
	-Ejercico 85(ajax es una función parecida a getJSON, la diferencia es que coge tanto JSON como XML)
		(JSON.stringify --  transforma el JSON en un string)(hay funciones por la red que te ponen el JSON en bonito, se pueden utilizar)
		(new Date(meteo.datos.sys.sunrise *1000).toLocaleTimeString() ----> para pasar a la fecha actual) 
	-Ejercicio 86(mola porque tiene todos los archivos separados, como nos lo piden)(dentor de  success: function(datos){ no funciona el this)
	(No vale presentar esto como ejercicio 2, hay que arreglarlo, ponerlo bonito css, etc, se puede consumir de este sitio o del mismo)