La principal diferencia a la hora de generar el Schema automáticamente frente a construirlo a mano es que el generador automático
no es capaz de distinguir tipos y por defecto pone todos los tipos a string. En este caso, los elementos:
páginaInicial, páginaFinal y año los ha puesto como string, cuando deberían ser integer.